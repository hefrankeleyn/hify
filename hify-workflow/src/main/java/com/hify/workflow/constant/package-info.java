/**
 * 工作流的模块级常量与枚举。
 * <p>🔴 不允许魔法值直接出现在代码中——状态值、次数上限、缓存 key 前缀等一律定义在这里。
 * <p>🔴 不要用一个类装下所有常量，按功能分类。
 * <p>枚举类名以 {@code Enum} 结尾、成员全大写，且<b>每个枚举字段必须有注释</b>。
 * <p>{@code long} 赋值用大写 {@code L}（小写 {@code l} 易与 {@code 1} 混淆）。
 */
package com.hify.workflow.constant;

# 前端环境变量模板。
#
# ⚠️ 仓库根 .gitignore 第 70 行忽略了 .env，所以真正生效的 .env 不入库。
# 本文件是它的可入库副本，新克隆时 `cp .env.example .env` 即可。
#
# 代码里对这两个变量都写了与下方一致的默认值（见 utils/request.ts、router/index.ts），
# 因此不建 .env 也能直接跑起来——.env 只用于本地临时覆盖。

# 后端 API 前缀。⚠️ 只到 /api，不含版本号——api/*.ts 里要自己带 /v1。
# 开发环境：由 vite.config.ts 的 server.proxy 转发到 http://localhost:8080
# 生产环境：由 Nginx 反代到 hify-app（CLAUDE.md 7.2）
# 两边都走相对路径，因此不区分环境，只有一份配置。
VITE_API_BASE_URL=/api

# 应用标题，用于浏览器标签
VITE_APP_TITLE=Hify 管理控制台
